{
  "name": "lofiai",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "cd front-end && npm install && cd ../back-end && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "cd back-end/server && npm start",
    "start:client": "cd front-end && npm start",
    "start": "concurrently \"npm run start:server\" \"npm run start:client\""
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "^9.4.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
