{
  "name": "lofiai",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "cd front-end && npm install && cd ../back-end && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node back-end/server/server.js",
    "heroku-postbuild": "cd front-end && npm run build"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "^9.4.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
